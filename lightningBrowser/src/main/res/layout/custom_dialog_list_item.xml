<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="15dp" >
    
   <LinearLayout 
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_toLeftOf="@+id/custom_dialog_list_item_selected_img"
        android:orientation="vertical"
        android:layout_centerVertical="true" >
        
        <TextView
		    android:id="@+id/custom_dialog_list_item_resolution_name"
		    android:layout_width="fill_parent"
		    android:layout_height="wrap_content"
		    android:textColor="@color/custom_dialog_text_color"
		    android:textSize="18sp"
		    android:gravity="center_vertical" />	
    </LinearLayout>

    <ImageView
	    android:id="@+id/custom_dialog_list_item_selected_img"
	    android:layout_width="wrap_content"
	    android:layout_height="wrap_content"
	    android:layout_alignParentRight="true"
	    android:contentDescription="@string/app_name"
	    android:layout_centerVertical="true"
	    android:background="@drawable/icon_device_selected"
	    android:visibility="invisible"
	    android:layout_marginLeft = "5dp" />	
</RelativeLayout>
